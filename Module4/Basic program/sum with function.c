#include<stdio.h>

int add(int a,int b){
	return a+b+c;
	
}
int mul(int a, int b){
	return a*b;
}

int main(){
	int mu = mul(10,20);
	int sum = add(10,20);
	printf("\n mul of three number is :%d",mu);
	return 0;
}
